import { RuinsConfigInternal } from "@ruins/config";
import { CommitSettings } from "../index.js";

export const scanCommits = (settings: CommitSettings, config: RuinsConfigInternal) => {
  console.log("TODO: get commits");
};
